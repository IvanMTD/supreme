create table if not exists Minio_File (
    id int GENERATED BY DEFAULT AS IDENTITY,
    post_id int,
    uid text,
    name text,
    type text,
    e_tag text,
    bucket text,
    path text,
    minio_url text,
    file_size int
);

create table if not exists Sport_Tag (
    id int GENERATED BY DEFAULT AS IDENTITY,
    user_ids integer[],
    post_ids integer[],
    image_id int,
    name text,
    description text
);

create table if not exists Post (
    id int GENERATED BY DEFAULT AS IDENTITY,
    user_id int,
    sport_tag_ids integer[],
    user_save_list integer[],
    file_id int,
    image_id int,
    name text,
    annotation text,
    content text,
    placed_at date,
    verified bool,
    allowed bool
);

create table if not exists App_User (
    id int GENERATED BY DEFAULT AS IDENTITY,
    sport_tag_ids integer[],
    post_ids integer[],
    post_save_list integer[],
    roles text[],
    mail text unique not null,
    password text not null,
    first_name text,
    middle_name text,
    last_name text,
    birthday date,
    placed_at date,
    phone text
);

create table if not exists Slider (
    id int GENERATED BY DEFAULT AS IDENTITY,
    title text,
    url text,
    image_id int
);

create table if not exists Event (
    id int GENERATED BY DEFAULT AS IDENTITY,
    content text,
    subject text,
    status text,
    city text,
    location text,
    ekp int,
    start_date date,
    end_date date,
    placed_at date
)