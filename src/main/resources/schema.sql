create table if not exists Minio_File (
    id int GENERATED BY DEFAULT AS IDENTITY,
    uid text,
    name text,
    type text,
    e_tag text,
    bucket text,
    path text,
    minio_url text,
    file_size float
);